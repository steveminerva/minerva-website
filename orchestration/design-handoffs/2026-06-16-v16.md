# Design handoff — v16 (export 2026-06-16)

> Reconstructed by Cowork from the package `claude design updates/Minerva website (16).zip`
> (internal brief `DEPLOYMENT-BRIEF-v17.md`). Verified against the live `steveminerva/minerva-website`
> repo. Full plan: `deployment/Minerva v16 Merge Plan.(md|pdf)`.

## 1. Summary
Large front-end refresh: SEO/sharing layer, an expanded Heritage member experience (Notifications,
Profile/Account with auto-renew + cancellation, News publishing tags) and a 3× larger admin console
(Automations, Subscriptions/Invoices, User groups, Roles & permissions, dashboard widgets, `tbl-std`
table layout). Data layer in the export is localStorage; no backend schema changed.

## 2. Scope confirmation — ⚠️ NOT all met (flag for future drops)
- [x] New `data-i18n` keys present in all 8 languages.
- [ ] **No hard-coded credentials** — ✗ `Minerva@1897` present in `assets/vip.js` and `0001_init.sql`.
- [ ] **No prototype data layer** — ✗ exported `vip.js` is localStorage (71 refs); `concierge-knowledge.js` is a client KB.
- [ ] **One canonical tree** — ✗ package also contains a stale `dist/`, a nested duplicate
  `Minerva website (16)/`, and design artifacts (`*.jsx`, preview/mockup HTML).
- [ ] **Version naming consistent** — ✗ zip says "(16)", brief says "v17".

## 3. Changed / new files (verified diff vs live)

**Safe / design-owned (take as-is):**
- `assets/i18n.js` (adds `nav.heritage`), `assets/minerva.css`
- New brand assets: `assets/og-share.png`, `assets/favicon-32.png`, `assets/favicon-64.png`, `assets/apple-touch-icon.png`
- Public pages: `index, history, news, press, investors, archives, team, models, privacy, terms` (SEO/OG head, Heritage menu, content)
- `assets/geo-map.js`, `assets/worldmap.svg` (if portal map is used)
- No-op (identical to live): `config.js`, `image-slot.js`, `legal-i18n.js`

**Wiring-bearing (markup/UI only — keep live engine + bridge):**
- `assets/admin-app.js` — 112 KB → **322 KB**; new console sections. Needs `admin-bridge.js` extended.
- `assets/admin-bridge.js` — ~same as live; extend for the new sections.
- `assets/heritage-i18n.js` — member + 8-lang email dictionary (take).
- `login.html, register.html, admin.html, portal.html, about.html, aegis.html, sovereign.html` — merge markup, restore wired script order.

**New pages:**
- `account.html` (member Profile/Account), `register.html` (Heritage registration page).

**Dropped / do not ship:**
- `assets/vip.js` (localStorage prototype + hard-coded pw) → keep live Supabase `vip.js`.
- `assets/minerva.js` (prototype) → keep live.
- `assets/concierge-knowledge.js` → superseded by server-side `concierge_kb` + `concierge` function.
- `dist/`, nested `Minerva website (16)/`, all `*.jsx` and preview/mockup HTML, `supabase/migrations/0001_init.sql`.

## 4. New features
Member **Notifications centre** (bell badge, dropdown, page); **Profile/Account** (membership
status, **auto-renew**, **mailing-permission**, **cancel membership** + confirmation email);
**News publishing tags** (Public / Private / Push); admin **Automations, Subscriptions/Invoices/
Payments, User groups, Roles & permissions, dashboard widgets, bulk actions, `tbl-std`** layout;
SEO/favicon/OG across public pages; Heritage menu item + Heritage page credit (vzw + Jean Claes).

## 5. Features that need backend wiring
- **Member notifications** — entity: member notifications; audience: members by tier; action:
  read/mark-read + admin push. Currently `localStorage` (`minerva_member_notifs_v3`) → needs a table + RLS.
- **Push notifications** — needs a real push/mail gateway (decision emails already send server-side).
- **Subscriptions / Invoices / Payments** — entity + audience + action TBD; prototype-only.
- **News Public/Private tags** — public `news.html` does not yet filter by tag; wire if it should gate the marketing site.

## 6. Open questions for Cowork / Steve
1. Confirm "(16)/v17" is the newest intended drop.
2. Canonical domain for OG/canonical URLs (`www.minervaluxurymotors.com` vs current GitHub Pages URL).
3. Sequence Tier 3 backend work (notifications table, push) now or after the public + member-page merges?

---

## Merge outcome (filled in by Cowork after the merge)
- Shipped: _pending_
- Held / deferred: _pending_
- Backend work created: _pending_
- Deployment-log version: _pending (next: v0.13)_
